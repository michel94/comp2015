program test(output);
var d, e: real;
	i: integer;

function main(var a: real; var o: integer) : real; FORWARD;

function main;
	begin
		a := 3;
		o := 2;
	end;

begin
	e := main(d, i);
	writeln(d, ' ', i);
end.
