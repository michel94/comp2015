
program gcd2(output);
var x: integer;
	y : real;
	a, b : BOOLEAN;
begin
	a := a;
	a := b aNd a;
	x := z dIv b;
	x := (x * x + x * 2);
	a := (x * 3 - 4.2 > 3.1);
	x := -2;
	b := false and true or a;
	x := 15 / 4.0;
end.

