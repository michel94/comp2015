
program gcd2(output);
var x, y: real;
function max(a: integer; var b: boolean): real; forward;
function dev(a: real): real;
	var o : real;
	begin
		a := 0
	end;
function max;
	var o : real;
		f : real;
	begin
		a := 0
	end;

begin
	if paramcount >= 2 then
		begin
			val(paramstr(1), x);
			val(paramstr(2), y);
			writeln(gcd(x, y))
		end
	else
		writeln('Error: two parameters required.')
end.

