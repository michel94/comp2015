program gcd(output);
var a, b: integer;
	abc: real;
	real: real;

function asd(boolean: boolean; a: integer): integer; FORWARD;

function asd;
	var b : boolean;
		integer: boolean;
		false: boolean;

	begin
		b := boolean AND integer;
	end;

begin
end.
