
program gcd2(output);
var x, y: integer;
function max(a: integer): real; forward;
function dev(a: integer): real;
	var o : boolean;
	begin
		a := 0
	end;
function max(a: integer): real;
	var o : boolean;
	begin
		a := 0
	end;

begin
	if paramcount >= 2 then
		begin
			val(paramstr(1), x);
			val(paramstr(2), y);
			writeln(gcd(x, y))
		end
	else
		writeln('Error: two parameters required.')
end.

